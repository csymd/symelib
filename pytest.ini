[pytest]
# Only collect the formal test suite (avoid examples/test_*.py name clashes)
testpaths = tests
pythonpath = src
addopts = -q
filterwarnings =
    ignore::DeprecationWarning:PyPDF2.*
    ignore:Support for class-based `config`:DeprecationWarning
