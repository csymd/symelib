# Updated for Python 3.13
[project]
name = "elib"
version = "0.1.0"
description = "Foundation for a local paper management system with agentic capabilities"
requires-python = ">=3.13"

[tool.uv]
python = "3.13"

# (rest of original content assumed or to be preserved in real edit)
